<UserControl x:Class="Labb3_Quiz.Views.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Labb3_Quiz.Views"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="Root"
             PreviewKeyDown="UserControl_PreviewKeyDown" >

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="VisibilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="{Binding ActivePack.Name}" Grid.Row="0" Grid.Column="0" Margin="20 10" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="0" Grid.Column="1" Margin="10 10">
            <Button Content="{fa:Icon Sliders}" Command="{Binding OpenPackOptionsDialogCommand}"/>
            <Button Content="{fa:Icon Plus}" Margin="5 0" Command="{Binding AddQuestionCommand}"/>
            <Button Content="{fa:Icon Minus}" Command="{Binding RemoveQuestionCommand}"/>
        </StackPanel>

        <ListBox 
            Grid.Row="1" 
            Grid.Column="0" 
            Grid.ColumnSpan="2" 
            Margin="20 0 10 20" 
            ItemsSource="{Binding ActivePack.Questions}" 
            SelectedItem="{Binding ActiveQuestion}" 
            >

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock Text="Question:" FontWeight="Bold" />
                        <TextBlock Text="{Binding Query}" TextWrapping="Wrap"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            
        </ListBox>

        <StackPanel Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" Margin="10 -8 20 0">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="IsEnabled" Value="True"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ActiveQuestion}" Value="{x:Null}">
                            <Setter Property="IsEnabled" Value="False"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            
            <Label Content="Question:" />
            <TextBox Height="70" Margin="0 0 0 15" Text="{Binding ActiveQuestion.Query, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"  />

            <Label Content="Correct Answer:" />
            <TextBox Margin="0 0 0 5" Text="{Binding ActiveQuestion.CorrectAnswer, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <Label Content="First Incorrect Answer:" />
            <TextBox Margin="0 0 0 5" Text="{Binding ActiveQuestion.IncorrectAnswer1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Content="Second Incorrect Answer:" />
            <TextBox Margin="0 0 0 5" Text="{Binding ActiveQuestion.IncorrectAnswer2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Content="Third Incorrect Answer:" />
            <TextBox Text="{Binding ActiveQuestion.IncorrectAnswer3, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
    </Grid>
</UserControl>
